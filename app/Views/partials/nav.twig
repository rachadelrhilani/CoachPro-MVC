<nav class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16 items-center">

            <a href="{{ base_url }}/" class="text-xl font-bold text-indigo-600">
                CoachPro
            </a>

            {% if session.user is defined %}
                <div class="flex space-x-4 items-center">

                    {% if session.user.role == 'sportif' %}
                        <a href="{{ base_url }}/sportif/coaches" class="nav-link">Coachs</a>
                        <a href="{{ base_url }}/sportif/seances" class="nav-link">Séances</a>
                        <a href="{{ base_url }}/sportif/history" class="nav-link">Mes réservations</a>
                    {% endif %}

                    {% if session.user.role == 'coach' %}
                        <a href="{{ base_url }}/coach/profile" class="nav-link">Profil</a>
                        <a href="{{ base_url }}/coach/seances" class="nav-link">Mes séances</a>
                        <a href="{{ base_url }}/coach/reservations" class="nav-link">Réservations</a>
                    {% endif %}

                    <a href="{{ base_url }}/logout" class="text-red-500 font-semibold">
                        Déconnexion
                    </a>
                </div>
            {% else %}
                <div class="space-x-4">
                    <a href="{{ base_url }}/login" class="nav-link">Connexion</a>
                    <a href="{{ base_url }}/register" class="nav-link">Inscription</a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>

<style>
.nav-link {
    @apply text-gray-700 hover:text-indigo-600 font-medium;
}
</style>
